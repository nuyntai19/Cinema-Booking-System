# Route all requests to index.php (including OPTIONS)
RewriteEngine On
RewriteBase /backend/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
